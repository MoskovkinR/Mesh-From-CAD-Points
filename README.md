# Mesh-From-CAD-Points
Mesh From CAD Points  Generate terrain meshes directly from CAD elevation text points inside Blender.  This addon reads elevation values from FONT objects and creates a terrain surface automatically using Delaunay triangulation or Grid generation methods.
